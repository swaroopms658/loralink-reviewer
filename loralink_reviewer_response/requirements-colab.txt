# Colab Free already ships working versions of torch, transformers, accelerate,
# scipy, pandas, matplotlib, numpy and psutil. Pinning/downgrading those forces
# pip's resolver into a ~20-min backtrack against Colab's CUDA torch build, so we
# leave them alone and install ONLY what Colab lacks. `datasets` is pinned
# because the 3.x line dropped script-based dataset loading that GEM/e2e_nlg
# still uses.
datasets==2.21.0
peft==0.12.0
evaluate==0.4.2
rouge-score==0.1.2
sacrebleu==2.4.3
zstandard==0.23.0
